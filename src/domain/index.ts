/**
 * Domaine — logique métier pure (zéro React, zéro Supabase).
 * Les fichiers de calcul (ranking.ts, stats.ts, badges.ts, match-utils.ts)
 * seront ajoutés aux sprints 2-5.
 */

export type {
  PlayerMatchResult,
  RankedPlayer,
  PlayerStats,
  HeadToHeadResult,
  SetScore,
  MatchFormData,
  BadgeDisplay,
  LeaguePlayerSummary,
} from "./types";
